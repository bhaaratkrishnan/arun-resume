---
const navBarElements = [
  "home",
  "about",
  "work",
  "education",
  "skills",
  "projects",
  "references",
];
---

<div class="flex flex-row space-x-10 mx-auto justify-center items-center bg-zinc-700">
    {
        navBarElements.map((item) => (
            <div class="uppercase text-md font-bold py-5 text-white 
            hover:text-[#157dec] transition ease-in-out  
            hover:-translate-y-1  duration-300">{item}</div>
        ))
    }
</div>
